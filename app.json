{
  "name": "neurobot_buddy",
  "description": "FastAPI backend for mobile app",
  "scripts": {
    "start": "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
  },
  "env": {
    "PORT": {
      "description": "Port to run the app",
      "value": "8000"
    }
  }
}